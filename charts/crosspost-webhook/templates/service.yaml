apiVersion: v1
kind: Service
metadata:
  name: {{ include "crosspost-webhook.fullname" . }}
  labels:
    {{- include "crosspost-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crosspost-webhook.selectorLabels" . | nindent 4 }}
