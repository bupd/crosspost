apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crosspost-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "crosspost-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command:
        - wget
      args:
        - -qO-
        - http://{{ include "crosspost-webhook.fullname" . }}:{{ .Values.service.port }}/healthz
